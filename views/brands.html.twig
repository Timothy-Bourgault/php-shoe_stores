<!DOCTYPE html>
<html>
  <head>
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.min.js" type="text/javascript"></script>
    <title>Shoe Genie</title>
  </head>
  <body>
    <div class="container pageMargin">
      <div class="jumbotron">
        <hr><hr>
        <h1 class="header">Shoe Genie</h1>
        <hr>
        <hr>
        <div class="row">
          <div class="col-sm-3">
          </div>
          <div class="col-sm-6">
              <h2>List of Brands In Town:</h2>
              {% if brands is not empty %}
                <ul>
                  {% for brand in brands %}
                    <li><a href="/get_brand/{{ brand.getId }}">{{ brand.getName }}</a>
                    </li>
                  {% endfor %}
                </ul>
              {% else %}
                <h3>There aren't any Brands listed yet...</h3>
                <h3>Enter some into the list with the form below...👇</h3>
              {% endif %}
              <form class="clearButton" action="/brands/delete_all" method="post">
                <button type="submit" class="btn-md btn-danger">Clear All Brands</button>
              </form>
              <h2>Add a New Brand to Our Directory:</h2>
              <form class="styleForm" action="/add_brand" method="post">
                <label for="brand_name">
                <input class="form-control" id="brand_name" type="text" name="brand_name" placeholder="Brand Name..."></label>
                <br>
                <br>
                <button class="btn-success btn-lg" type="submit">Add Brand</button>
              </form>
          </div>
          <div class="col-sm-3">
          </div>
        </div>
        <hr><hr>
        <p class="returnHome">
          <a href="/">👈🏽 Go To Shoe Genie Home Page</a>
        </p>
      </div>
    </div>
  </body>
</html>
