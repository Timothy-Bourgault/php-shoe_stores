<!DOCTYPE html>
<html>
  <head>
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.min.js" type="text/javascript"></script>
    <link href="https://fonts.googleapis.com/css?family=Bitter|Bree+Serif|Shrikhand" rel="stylesheet">
    <title>Shoe Genie</title>
  </head>
  <body>
    <div class="container pageMargin">
      <hr><hr>
      <h1>Shoes In Town</h1>
      <hr>
      <hr>
      <div class="row">
        <div class="col-sm-6">
          <h2>List of Stores In Town:</h2>
          {% if stores is not empty %}
            <ul>
              {% for store in stores %}
                <li><a href="/get_store/{{ store.getId }}">{{ store.getName }}</a>
                </li>
              {% endfor %}
            </ul>
          {% else %}
            <h3>There aren't any Stores listed yet...Enter some into the list with the form below...👇</h3>
          {% endif %}
          <form class="clearButton" action="/stores/delete_all" method="post">
            <button type="submit" class="btn-lg btn-danger">Clear All Stores</button>
          </form>
          <h2>Add a New Store to Our Directory:</h2>
          <form class="styleForm" action="/add_store" method="post">
            <label for="store_name">
            <input class="form-control" id="store_name" type="text" name="store_name" placeholder="Name..."></label>
            <br>
            <br>
            <button class="btn-success btn-lg" type="submit">Add Store</button>
          </form>
        </div>
        <div class="col-sm-6 form-group">
            <h2>List of Brands In Town:</h2>
            {% if brands is not empty %}
              <ul>
                {% for brand in brands %}
                  <li><a href="/get_brand/{{ brand.getId }}">{{ brand.getName }}</a>
                  </li>
                {% endfor %}
              </ul>
            {% else %}
              <h3>There aren't any Brands listed yet...Enter some into the list with the form below...👇</h3>
            {% endif %}
            <form class="clearButton" action="/brands/delete_all" method="post">
              <button type="submit" class="btn-lg btn-danger">Clear All Stores</button>
            </form>
            <h2>Add a New Store to Our Directory:</h2>
            <form class="styleForm" action="/add_brand" method="post">
              <label for="brand_name">
              <input class="form-control" id="brand_name" type="text" name="brand_name" placeholder="Name..."></label>
              <br>
              <br>
              <button class="btn-success btn-lg" type="submit">Add Store</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
