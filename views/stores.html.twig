<!DOCTYPE html>
<html>
  <head>
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.min.js" type="text/javascript"></script>
    <title>Shoe Genie</title>
  </head>
  <body>
    <div class="container pageMargin">
      <div class="jumbotron">
        <hr><hr>
        <h1 class="header" >Shoe Stores In Town</h1>
        <hr>
        <hr>
        <div class="row">
          <div class="col-sm-3">
          </div>
          <div class="col-sm-6">
              <h2>List of Stores In Town:</h2>
              {% if stores is not empty %}
                <ul>
                  {% for store in stores %}
                    <li><a href="/get_store/{{ store.getId }}">{{ store.getName }}</a>
                    </li>
                  {% endfor %}
                </ul>
              {% else %}
                <h3>There aren't any Stores listed yet...</h3>
                <h3>Enter some into the list with the form below...👇</h3>
              {% endif %}
              <form class="clearButton" action="/stores/delete_all" method="post">
                <button type="submit" class="btn-md btn-danger">Clear All Stores</button>
              </form>
              <h2>Add a New Store to Our Directory:</h2>
              <form class="styleForm" action="/add_store" method="post">
                <label for="store_name">
                <input class="form-control" id="store_name" type="text" name="store_name" placeholder="Store Name..."></label>
                <br>
                <br>
                <button class="btn-success btn-lg" type="submit">Add Store</button>
              </form>
          </div>
          <div class="col-sm-3">
          </div>
        </div>
        <hr><hr>
        <p class="returnHome">
          <a href="/">👈🏽 Go To Shoe Genie Home Page</a>
        </p>
      </div>
    </div>
  </body>
</html>
